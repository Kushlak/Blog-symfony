{% extends 'base.html.twig' %}

{% block body %}
    <h1>Update Post</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Update') }}</button>
    {{ form_end(form) }}
{% endblock %}
